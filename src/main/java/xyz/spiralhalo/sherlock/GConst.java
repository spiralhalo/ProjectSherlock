package xyz.spiralhalo.sherlock;

public class GConst {
    private static final int ONE_SECOND = 1000;
    public static final int TRACKER_DELAY_MILLIS = ONE_SECOND; // tracker granularity has to be in unit of seconds (not millisecond) due to record data constraint
}
